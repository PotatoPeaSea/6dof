# Firmware

Placeholder. V1 of the simulator runs without arm firmware — keyboard input drives the virtual iron.

When firmware lands here, it MUST produce the wire format defined in `../../docs/serial-protocol.md`. Don't change that protocol without updating `Software/Assets/Scripts/Input/SerialIronInput.cs` in the same change.
